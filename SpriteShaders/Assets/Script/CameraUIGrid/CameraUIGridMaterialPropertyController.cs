using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class CameraUIGridMaterialPropertyController : MonoBehaviour
{
    // [SerializeField] private RawImage rawImage;
    // private MaterialPropertyBlock _materialPropertyBlock = new MaterialPropertyBlock();
    // private const string strRows = "_Rows";
    // private const string strColumns = "_Columns";

    private void Start()
    {
        // _materialPropertyBlock.SetFloat(strRows, 0);
        // _materialPropertyBlock.SetFloat(strColumns, 0);

        // rawImage.SetPropertyBlock(_materialPropertyBlock);
    }
}
